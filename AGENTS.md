# 本项目协作规则

## 项目与上下文

- 本仓库是两个商用订单管理系统的测试工作台，不是产品源码仓库。
- 使用简体中文，说明结论的依据及未确认事项。没有用户要求时不启动子代理。
- 新需求先读取 `README.md`、相关 `context/` 文档及对应需求目录；缺失信息按需询问，不要求用户重复已确认的背景。
- 一个需求一个目录、一个主要会话；跨客户端和管理端的同一需求保留在一起。
- 用户只授权讨论时，停留在分析阶段；编写用例、执行测试、知识维护分别按用户当次指令推进。
- 需求流程和交付遵循 `context/workflow.md`：正式交付围绕思维导图用例、知识库内容及用户临时指定的产物，不默认增加成套报告。

## 会话分工与持续改进

- 项目治理会话负责测试行为、方法、遗漏、框架和交接质量的审计；当前建立本仓库的会话为治理入口，不承接具体需求任务。
- 需求会话负责指定需求的分析、用例、执行和知识沉淀。仅在用户明确要求创建新会话时创建；开场按 `templates/session-opening.md`，只携带已知事实和已授权阶段。
- 当前规范为试行基线，尚未通过完整需求周期验证。遵循 `governance/README.md`，根据实际使用中的收益和问题改进，不把初始框架描述为最佳实践。
- 在需求分析阶段结束、用例评审完成、测试收尾时进行简短流程复盘，并提醒用户关注本阶段实际暴露的改进点。无新增问题时注明即可，不重复长问卷，也不阻塞用户已授权的下一步。
- 需求会话将改进发现及证据记录在本需求 README 的“流程复盘”中；跨需求规范的调整由治理会话综合评估，避免多个需求会话同时改写通用规则。用户明确要求调整通用规范时按其要求执行。
- 发现已执行步骤遗漏关键业务校验、测试结论缺少证据或知识来源冲突时，当阶段立即指出，不等收尾。提醒必须基于本次证据，不能推测未查看会话的行为。
- 治理会话按用户要求读取需求产物或其他会话进行审计；阶段提醒由正在工作的需求会话执行。未建立自动化时，不声称会在其他会话结束后自行醒来、持续监听或自动通知。

## 分析与用例

- 区分原始需求、知识库现行规则、用户确认结论、推断、待确认项和实际观察。历史用例只作参考，不能直接证明当前需求规则。
- 对依赖既有项目规则的问题，优先读取对应知识库。先按实际工具清单确认能力；读取为空不等于可以自行补造规则。
- 未明确的验收条件单独记录；不得用“正常”“符合预期”等模糊描述替代可核对的结果。
- 正式用例采用思维导图，遵循 `context/test-cases.md`。用户要求使用 Mind 时，通过 `asynctest-mind` 读取或编辑，不离线重写已打开文档。

## 执行与证据

- 执行前明确系统、环境、版本（未知则注明）、账号角色、测试范围与必要数据；测试环境网络不可达时记录阻塞，不自动切换环境。
- UI 和接口测试均应关联需求、用例、前置条件、操作或请求、预期和实际结果。涉及写操作时核对执行后的业务状态，不能仅凭 HTTP 200 或成功提示判定通过。
- 状态至少区分未执行、通过、失败、阻塞；无法访问或缺少数据不能标记通过。
- MCP 连通性、工具列表可见、原型审阅和历史用例标记均不能作为业务测试通过证据。
- 复现问题时先定位页面、参数、接口与业务规则，保留请求和响应等必要证据，避免无依据地判定缺陷归属。
- 凭据和原始会话数据只在本机使用，不写入跟踪文件、知识库正文或交接说明。提交前检查证据脱敏；`.gitignore` 不是自动脱敏机制。

## 知识维护与交接

- 知识库维护按用户明确范围进行；使用 `context/tools.md` 中的项目映射，调用时再次核对返回的项目身份。
- 写入前准备具体内容；如 MCP 返回确认流程，遵循其预览和确认机制。只有成功回执后才能记录“已同步”，并按返回引用核对现行内容。
- 将跨端规则的主条目和关联引用说清楚，避免两边重复维护却不同步。
- 阶段结束更新需求 `README.md` 的状态、已确认结论、待确认问题、产物入口和下一步。执行细节有内容后放入 `runs/`，知识同步信息有内容后放入 `knowledge-sync.md`。
- 保留用户现有修改，不把本仓库文档写入全局个人记忆。Git 提交与同步遵循下方持续授权；部署和发布仍需用户明确指示。

## Git 与工作目录

- 用户已授权经常提交和 push：完成一个有意义的工作单元、阶段资料更新或会话收尾时，检查本次修改并及时提交、推送到 `origin/main`，不必每次再次询问。用户当次要求不提交、不推送或指定其他分支时，以当次要求为准。
- 只提交本次明确负责且已核对的文件，避免混入其他会话或用户尚未完成的修改。共享目录中提交前重新检查暂存区与分支。
- 推送成功后核对远端状态；失败时如实说明未同步的提交及原因，不强制推送，不覆盖远端历史。
- **任何时候都不得创建 worktree，除非用户明确要求。** 新需求会话直接使用保存的本地项目目录；使用创建会话工具时选 `environment: { type: "local" }`，不得自动选择工作树。
- 多个会话共用本地目录时保留其他会话的修改，避免同时编辑同一通用文件；需要隔离时先协调工作范围，不能自行创建 worktree。
