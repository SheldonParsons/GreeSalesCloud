# P3 原型需求文字摘录

- 页面：【26.09.30】机型非标点-添加型号-gj
- 读取日期：2026-09-21；原型资源 revision：9。
- 原型入口：https://ued.gree.com:6060/run/prototype/IOhb8IoTEbD/xl49pZvQnl/RlLQ6_BkAmeo
- HTML SHA-256：`ddacd9af10a39d6c371ee9064f16c2059e1376210b1f1b9c5434f4026520546e`
- 读取方式：通过该原型页面实际调用的只读预览数据取得对应 Axure HTML，抽取最新需求段中的静态文字；未执行页面交互。
- 边界：页面同时含历史版本，本文件在下一历史版本段前截取；旧版全文留在本机 `.local/69610/`。文本顺序按源HTML，不等同视觉布局；多条规则在同一段时保留源文连接。外部模板未读取，链接访问参数已移除。
- 原文中的旧口径或笔误不直接变成现行规则；已确认差异以需求稿及知识确认索引为准。

## 需求文字

需求说明
1.表单填写-非标点信息
版本说明
1、添加机型非标点的配置对应的配置状态参数；
2、增加配置状态逻辑校验规则；
编号
属性名称
控件类型
字段长度
约束条件及说明
备注
1
选择型号
按钮
弹框显示当前商品中心型号列表
当用户选择型号后，该按钮隐藏
2
关联型号与非标点配置
列表
-
配置当前型号、生产用编码以及对应增减物料逻辑关系
-
3
取消
按钮
取消当前操作
4
确定
按钮
确定当前操作
2.选择型号
3.关联型号与非标点配置
1、通过生产用编码检索对应信息在库型号；
2、展示录入生产用编码后的商品型号以及对应的商品编码
通过勾选后的型号会进入配置列表中
编号
属性名称
控件类型
字段长度
约束条件及说明
备注
1
添加非标点
按钮
给当前机型增加非标点，在当前型号下新增一条记录
当前单一商品型号仅允许添加10条非标点
2
生产用编码
文本框
1、自动剔除生产用编码中的字符`和空格【自动修正生产用编码】
【校验生产用编码】（以修正后的进行校验）
必填，支持英文、数字；不符合要求，则错误提示：不符合填入要求（必填，支持英文、数字）需判断生产用编码第一个字符是否为字母；不符合要求，则错误提示：设备的生产用编码需字母开头2、先根据生产用编码查出库内对应生产用编码状态：
检测当前生产用编码是否存在商品库中，若不存在，则错误提示：当前生产用编码不存在；检测当前生产用编码在商品库中对应多个型号，则错误提示：当前生产用编码对应多个商品型号；检测当前生产用编码对应的商品型号是否存在商品库中，若不存在，则错误提示：当前生产用编码对应的商品型号不存在；检验当前生产用编码在商品库中存储的信号代码是否有效，如无效，则错误提示：当前生产用编码已报废；说明：
设备/物料有效&报废的判断：
设备/物料有效：信号代码=空、002、030
设备/物料已报废：信号代码 != 空、002、030
点击生产用编码的删除按钮后，可删除当前已录入的生产用编码，同时对应的商品编码、商品型号以及非标点和对应的增减物料都删除，同时仅保留一行非标点录入；
2
型号
文本
展示录入生产用编码对应的商品型号
3
商品编码
文本
展示录入生产用编码对应的商品编码
4
非标点
下拉选择
必填，单选，选项为当前非标点管理列表；
1、同一型号非标点只能选择一个，再次对同一型号进行【添加非标点】，已经勾选配置过的非标点被过滤；
2、当切换当前非标点，后面对应的增减物料会清除
5
配置状态
下拉选择
必填，单选，选项为：已配置、未配置
1、默认勾选为是。
2、当用户选择为【未配置】后，增加物料和减少物料为禁用置空状态。
3、用户选择为【已配置】后，增加物料和减少物料为可编辑状态；
6
+增加物料
文本输入框
100
1、选填，支持英文、数字；
2、用户输入后，按回车进行二次确认，此时会进行bann系统物料校验，查到的物料编码对用物料名称回传后，会按照【物料编码-物料名称】的格式进行添加，作为文本标签添加，保留删除按钮；
3、成功录入的物料，后会跟输入框进行数量添加，默认为1，限制1~100；
4、最多添加10个物料；
编辑状态下可进行编辑
输入/编辑时，对接【物料管理】进行匹配，精确匹配；
【按回车后触发校验，匹配结果及处理如下】：
①无物料匹配，弹框提示：无匹配物料；
②有物料匹配，但设备/物料已报废，弹框提示：物料已报废；
以上都校验通过，则匹配成功
说明：
物料有效：信号代码=空、002、030
物料已报废：信号代码 != 空、002、030
物料正确性校验
需判断商品编码第一个字符是否为数字，若否，需弹框提示：请输入物料
重复校验：
1、需校验【-减少物料编码：】之间的物料编码是否有相同，若相同，需弹框提示：物料编码存在重复；
2、同一个录入框，录入相同物料编码，需弹框提示：物料编码存在重复；
7
-减少物料
文本输入框
100
1、选填，支持英文、数字；
2、用户输入后，按回车进行二次确认，此时会进行bann系统物料校验，查到的物料编码对用物料名称回传后，会按照【物料编码-物料名称】的格式进行添加，作为文本标签添加，保留删除按钮；
3、成功录入的物料，后会跟输入框进行数量添加，默认为1，限制1~100；
4、最多添加10个物料；
编辑状态下可进行编辑
输入/编辑时，对接【物料管理】进行匹配，精确匹配；
【失焦时匹配，匹配结果及处理如下】：
①无物料匹配，弹框提示：无匹配物料；
②有物料匹配，但设备/物料已报废，弹框提示：物料已报废；
以上都校验通过，则匹配成功
说明：
物料有效：信号代码=空、002、030
物料已报废：信号代码 != 空、002、030
物料正确性校验
需判断商品编码第一个字符是否为数字，若否，需弹框提示：请输入物料
重复校验：
1、需校验【+增加物料编码：】之间的物料编码是否有相同，若相同，需弹框提示：物料编码存在重复；
2、同一个录入框，录入相同物料编码，需弹框提示：物料编码存在重复；
8
移除
按钮
移除当前非标点行对应的增减物料配置
列表仅剩最后一条非标点行则移除按钮隐藏
4.确定
（1）进行相关检测：
检测必填项是否已填和各字段是否按要求填写：若校验不通过，各参数展示相应提示；校验当前提交的商品型号对应的生产用编码是否与系统中相同型号的生产用编码一致，如果不一致，则执行失败，并提示当前生产用编码不一致检测录入【生产用编码】对应的【商品型号】号，是否仍在【商品中心】同时商品型号为已启用状态，若不存在，则执行失败，并提示商品不存在。检验当【生产用编码】+非标点是否存在机型非标点列表中，若存在则执行失败（无论所处状态），并提示非标点重复。若以上内容均检测通过，且执行成功，则提示“机型非标点进入审批流程”。
机型非标点进入审批流程
新增失败
确 定
16
16
以下异常导致新增失败。

用户输入当前生产用编码，根据生产用编码反查对应的商品编码和商品型号
商品型号
TradeCode21
TradeCode21
TradeCode21
TradeCode21
非标点
TradeCode21
TradeCode21
TradeCode21
TradeCode21
失败原因
商品不在商品库中
当前非标点不存在
非标点已存在
当前非标点不存在
序号
1
2
3
4
5.审批通过后
（1）该条机型+非标点记录中对应的调整记录新增一条。
调整记录
序号
调整字段
原值
调整后值
调整人
调整时间
操作
1
非标点
-
增加铭牌
张三
2026.05.11
生产用编码
商品编码
型号
非标点
配置状态
+ 增加物料 (回车录入)     单一物料可调整数量
- 减少物料 (回车录入)  单一物料可调整数量
操作
LL3020240235
增加旁通管
移除
LL3020240231
移除
物料编码1-物料名称1
物料编码2-物料名称2
物料编码3-物料名称3
增加旁通管
增加冷媒隔离阀
离心机机组型号 (型号-1)
LL3020240231
1
1
1
添加非标点
物料编码4-物料名称1
1
已配置
未配置
生产用编码
商品编码
型号
非标点
配置状态
+ 增加物料 (回车录入)     单一物料可调整数量
- 减少物料 (回车录入)  单一物料可调整数量
操作
增加旁通管
